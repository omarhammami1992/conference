package com.soat.back.conference.query.application;

public record AddressJson(String fullAddress,
                          String city,
                          String country,
                          String latitude,
                          String longitude

) {
}
